interface:
  display_name: "Company Data Security"
  short_description: "Protect sensitive data at active data boundaries"
  default_prompt: "Use $company-data-security to minimize sensitive-data exposure in this task."
policy:
  allow_implicit_invocation: true
