interface:
  display_name: "Change Review"
  short_description: "Review changes against intent and risk"
  default_prompt: "Use $change-review to perform independent integration and adversarial review against a frozen intent and diff."
