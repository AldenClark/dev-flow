interface:
  display_name: "Dev Flow"
  short_description: "融合仓库规范、产品设计与验证证据的开发流程"
  default_prompt: "Use $dev-flow to discover scoped repository instructions, resolve repository facts, surface and disposition semantic ambiguity with the user as final requirement owner, bind the bounded requirements/design, implement them, and verify the result with fresh evidence."
