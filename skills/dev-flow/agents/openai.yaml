interface:
  display_name: "Dev Flow · Repository Engineering"
  short_description: "Guide repository work from intent to tested outcome"
  default_prompt: "Use $dev-flow to understand the outcome, implement the smallest complete change, preserve durable repository knowledge, and verify it with honest evidence."
policy:
  allow_implicit_invocation: true
