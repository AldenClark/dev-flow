interface:
  display_name: "Dev Flow · Repository Engineering"
  short_description: "Guide repository work from intent to tested outcome"
  default_prompt: "Use $dev-flow to reach the requested outcome, explore alternatives only where uncertainty changes a decision, verify the final behavior with the relevant native oracle, and report unrun environments honestly."
policy:
  allow_implicit_invocation: true
