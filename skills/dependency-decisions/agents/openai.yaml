interface:
  display_name: "Dependency Decisions"
  short_description: "Choose safe, reversible dependencies"
  default_prompt: "Use $dependency-decisions to assess a capability gap before choosing a dependency, service, or runtime."
