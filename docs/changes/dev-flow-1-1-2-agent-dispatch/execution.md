# Change execution: dev-flow-1-1-2-agent-dispatch

[Tracked change manifest](./manifest.json)

## Implementation

- SC-D1: added the versioned profile registry, vector selector, public CLI, explicit exception/downgrade guards, and fail-closed registry validation.
- SC-D2: bound pre-spawn routing and requested/effective receipt fields across orchestration and templates.
- SC-D3: added 12 frozen routing cases and focused cross-platform black/white tests.
- SC-I1: retained the six-cell matrix, added a focused native-Python dispatch step, deterministic UTF-8, and separate scalar methodology/knowledge commands without forcing a POSIX shell on Windows.
- SC-I2: advanced every current source/release/install surface to 1.1.2 and updated protected attestation identity.
- SC-P1/SC-P2/SC-O1: existing CLI, roles, methodology, evaluation, release, and authority boundaries remain compatible; no dependency or excluded delivery surface was added.
- SC-L1: commit/tag/push/install are authorized but remain pending final evidence.

## Status

The source implementation and local acceptance gates are complete. AC-1 through AC-5 and the local portions of AC-6/AC-7 passed; hosted CI, immutable artifact, remote identity, and primary installation remain post-commit delivery gates for AC-6 through AC-8. No model-behavior pilot is required because dispatch correctness is deterministic policy behavior; the existing supplemental evaluation harness is unchanged.
