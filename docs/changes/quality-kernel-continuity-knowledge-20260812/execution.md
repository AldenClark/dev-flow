# Execution for quality-kernel-continuity-knowledge-20260812

[Manifest](./manifest.json)

## Requirement and design baseline

- Authoritative requirement source: tracked [requirements.md](./requirements.md), bound by the manifest to `sha256:e0258dc14c581cd54d14ee424c4b01f6b7393096fb8ffc78ac814dac518d645f`; the new quality-tagged packet adopts these exact bytes at revision 1.
- Authoritative design source: tracked [design.md](./design.md), bound by the manifest to `sha256:d7d25fe3dc1e5591f42701e718968954f034a3f35ceb2cf066f02e923b314566`; the new quality-tagged packet adopts these exact bytes for content-bound approval.
- Repository base observed for this documentation slice: commit `34a94aa1153a46a00ffe7579c451c947b9341b4e` on branch `main`, with a shared working tree containing concurrent implementation edits.
- Runtime recovery state binds the active packet to exact requirement, design, engineering-context, scope, evidence, and checkpoint projections; this tracked dossier retains the sanitized semantic record rather than raw local state.

## Active objective and slice

The integrated objective is to complete AC-1 through AC-12 within SC-D1, SC-D2, SC-D3, SC-I1, and SC-C1 without weakening SC-P1, SC-O1, or SC-L1. The current documentation slice owns the dogfood portion of SC-D3: establish a tracked current-truth catalog and governed dossier that the knowledge validator can check against the actual repository.

## Ordered progress

1. The requirement revision chain, ambiguity rulings, acceptance criteria, scope, verification obligations, and selected architecture were persisted before final integration.
2. The shared working tree contains implementation for the quality-kernel tag, persistent-mutation routing, content-bound design approval, semantic checkpoint and resume behavior, repository identity/HEAD/worktree binding, knowledge binding and validation, and compatibility handling. These are implementation facts, not final acceptance claims.
3. Parallel owner work added repository-specific engineering-context discovery, knowledge-system implementation and tests, separate test-technique accountability, coherent-slice and comment guidance, and stronger multi-agent briefs. Root integration remains responsible for reconciling overlapping contracts.
4. This slice added the current project catalog, current governance document, and governed requirements/design/execution/verification dossier under the default knowledge roots.
5. Dedicated structural validation first exposed two broken source-anchor links. After correcting them to the repository's actual contract-check entry point, the actual-repository validator passed and the dedicated knowledge-system unit suite passed all 16 tests.
6. Integration inspection identified false drift for normal open-slice edits, silent HEAD rebasing, repeated full-worktree hashing in mutation hooks, custom knowledge-root replay loss, weak traced quality fields, and delivery-intent over-routing. Corresponding runtime, hook, template, CLI-binding, and routing-fixture edits exist in the shared worktree; their final integrated result remains subject to VO-1 through VO-6.
7. BLUE-3 authority review found that the tracked dossier reused `SC-*` IDs with meanings different from the active packet and overstated partial test runs as overall passes. The dossier was realigned to the packet's exact AC/SC/VO sets and meanings, while its extra decision detail was moved to the non-authoritative `TD-*` namespace.
8. Repair round 1 added an immutable creation authority contract, exact approval/ambiguity event projection, recoverable checkpoint invalidation, validator-recomputed engineering-context projection, populated-submodule byte observation, linked-worktree ignore resolution, and exact knowledge authority binding. Every first-review counterexample is now a dedicated regression.
9. The earlier change-set-only packet was moved intact to `.codex/dev-flow/archive/quality-kernel-continuity-knowledge-20260812-legacy-unbound/`. A new packet with the same change ID was created through the current CLI and must bind the exact tracked requirement/design bytes; no tag or approval was migrated in place.
10. The second frozen blue/red review confirmed the original repair matrix, then rejected repair completeness for tombstone payload drift, partial quality downgrade, unbound quality dossiers, cross-role stable-ID definitions, and cross-transition event reordering.
11. Repair round 2 made tombstones and ledger chronology exact, treats any current-quality residue as non-downgradable, requires authority-bound material dossiers at the immutable quality-packet boundary, and makes governed ID ownership global across requirements/design. Its 326-test gate passed before the next scoped review exposed the remaining architecture variants.
12. The third scoped review found the same roots across schema dispatch and ambiguity history. Architecture repair moved quality provenance before schema dispatch and replaced reverse lookups with one ordered lifecycle/requirement/ambiguity/checkpoint replay. Root passed 330/330 tests and every deterministic gate.
13. Final bounded blue and red confirmation independently returned `ACCEPT` on source digest `464ce9e71803c5121ce3c032835bbdf6b787f54b139b9a50500cd146b0904d84`, with no open finding. Reusable verified conclusions were then promoted to tracked current truth.

## Drift rulings

| Changed premise or proposal | Ruling | Effect on execution |
|---|---|---|
| Quantitative evaluation was initially a named direction. | It was removed as a core goal after user correction. | Retain deterministic structural checks and evidence, but do not add a composite score or optimize for proxy metrics. |
| Periodic rereading was proposed to prevent forgetting. | The goal is semantic continuity, not cadence. | Trigger recovery from lifecycle, compaction, handoff, baseline, context, or premise changes and bind it to digests. |
| A two-layer document model was proposed. | It was advisory rather than mandatory. | Use three semantic authority planes and permit an existing repository convention to select tracked roots. |
| The existing twelve-Skill topology was proposed for retention. | It was advisory rather than a permanent architecture constraint. | Preserve compatibility now, derive inventory from capability ownership, and allow later evidence-backed topology changes. |
| Critical quality controls were conditionally loaded. | False-negative routing could remove protection. | Move the minimum quality kernel into every persistent mutation and make specialist routing additive. |

## Last proven state

The earlier 301-test pass claim remains withdrawn because it did not reproduce on the first independent freeze. After architecture repair, the root integrator freshly ran 330/330 tests in 68.068 seconds, plus 39/39 contracts, plugin, maintainer, knowledge, compile, 96-file JSON, and diff gates; all exited 0. Final bounded blue and red review independently accepted the frozen source with no open finding. The change is `accepted`, and reusable verified conclusions are `promoted` to current truth.

## Next safe action and stop conditions

Implementation, semantic review, promotion, and promotion-only scoped confirmation are complete. Future edits to this accepted dossier require an explicit erratum or follow-up change; do not silently rewrite its history.

## Team ownership and resource leases

- Root integrator owns the lifecycle packet, cross-file reconciliation, full verification, review disposition, and final claims.
- This slice has exclusive write ownership only over `docs/project/` and `docs/changes/quality-kernel-continuity-knowledge-20260812/`.
- Other owners retain their assigned runtime, context/profile, testing/practice, Skill, template, governance, and evaluation paths; this slice does not revert or overwrite their work.
- No network service, device, port, database, deployment environment, credential, or exclusive test resource is leased by this documentation slice.
- Black-box responsibility for this slice is the actual-repository validator CLI. White-box responsibility is exercised by the dedicated validator unit tests. Integrated behavior remains a root-owned obligation.

[Current Dev Flow governance](../../project/dev-flow-governance.md)
