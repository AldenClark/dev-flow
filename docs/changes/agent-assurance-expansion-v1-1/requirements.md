# Change requirements: agent-assurance-expansion-v1-1

[Tracked change manifest](./manifest.json)

## Requirement source and understanding revisions

- Original input: deepen the AI programming/general-agent methodology research and, under delegated judgment without intermediate confirmation, implement the selected result completely and release Dev Flow `1.1.0` by commit, tag, push, and local Codex update.
- Current truth: extend the shared method registry and deterministic risk-to-method selector with distinct Agent failure models and progressive guidance; preserve existing owner, schema, context, authority, and evidence contracts; perform only the explicitly authorized delivery actions.
- Ambiguities: none material. GitHub Release, PR, deployment, force push, moving tags, dependencies, and unrelated cleanup are excluded rather than inferred from “release.”

## User and product outcome

Less-experienced users can report ordinary facts such as stale observations, AI patch overfit, prompt injection exposure, persistent memory, irreversible actions, or multi-agent work. Dev Flow selects a bounded professional method stack, artifact owner, prerequisite, fallback, limitation, and failure-sensitive evidence without requiring the user to name the theory.

## Requirement delta

Extend the accepted 94-method Assurance Method System with 17 distinct Agent methods, 14 narrowly scoped risk models, richer sources/guidance/tests, and release/install identity while preserving all public selector and owner contracts.

## Acceptance criteria

- AC-1: Registry inventory is exactly 111 methods, 67 referenced sources, and 32 risk models, including 17 new AI coding/general-agent methods and 14 focused Agent risk models.
- AC-2: New coverage spans autonomy, partial observability, receding-horizon/reactive planning, counterexample repair, runtime/temporal assurance, compensation, trajectory intervention, provenance, human allocation, memory lifecycle, multi-agent topology/allocation, conformal selection, and simulation.
- AC-3: Deterministic scenarios select intended new stacks and preserve formal-depth, negative, context-cap, and real-prerequisite boundaries.
- AC-4: Related existing Agent methods are strengthened without duplicate ownership or output-schema change.
- AC-5: Progressive and public docs explain the new failure models, source adaptations, novice protocol, and assurance boundary; every link resolves.
- AC-6: All existing deterministic contracts, data-security integrity, knowledge, plugin, release, and strict unit gates pass on final bytes.
- AC-7: All source/release surfaces consistently identify `1.1.0`, and two builds from the exact commit are byte-identical and verify.
- AC-8: The in-scope tree, remote `main`, immutable annotated `v1.1.0`, and primary installed Codex plugin/cache are verified at the released identity.

## Non-functional requirements

- Deterministic, offline after installation, standard-library-only selection with stable output and bounded context.
- Backward-compatible schema/CLI/owner behavior and cross-platform Python 3.11/3.14 contracts.
- Missing prerequisites, authority, external tools, live systems, and real-user evidence remain unresolved or `NOT RUN`.

## Compatibility and exclusions

- Additive schema `1.0` registry change; output remains `method.selection.v1`; no dependency, new top-level Skill, owner change, packet migration, or platform/runtime reduction.
- Method/tool/live/production evidence not actually run remains unresolved or `NOT RUN`.
- The original accepted methodology dossier remains frozen history; this change has its own follow-up dossier.

## Requirement Ready gate

- Status: ready under the user's explicit execute-mode delegation and delivery authority.
- Revision: 4; full file is the baseline. Revision 2 split five specialist failure mechanisms; revision 3 made exact authority portable to tracked knowledge; revision 4 records that the final commit must precede the sealed pre-verification checkpoint. User outcome and delivery authority are unchanged.
- Reopen: incompatible schema/owner/dependency need, unsafe remote divergence, immutable identity failure, or a verified finding that changes AC semantics.

## Requirement baseline

- Revision: 4.
- Digest: content-bound by `REQ-READY` after these exact bytes and all governed packet projections validate.
- Baseline content: this entire requirements document.
- Reopen conditions: the conditions listed in the Requirement Ready gate.

## Ambiguity ledger

| ID | Source and interpretations | Evidence | Materiality and owner | Affected IDs | Recommendation | Status and resolution |
|---|---|---|---|---|---|---|
| AMB-1 | Keep nine coarse Agent models with specialist cross-trigger risk, or split five specialist failure mechanisms into dedicated models. | Selector activates a method from its risk-model stack; targeted negative scenarios can distinguish HTN, contract-net, conformal, and simulation over-trigger. | material; Codex | AC-1, AC-3, AC-4 | Split to 14 new Agent models and add non-cross-trigger regression cases. | resolved by deterministic evidence under delegated design authority |
| AMB-2 | Keep condensed tracked authority documents, or promote the exact approved bytes with a portable dossier-relative link. | `bind-knowledge` rejected both condensed documents; exact authority replay requires byte identity. | material; Codex | AC-5, AC-6 | Use one full exact document on both surfaces with `./manifest.json`. | resolved by fail-closed knowledge evidence under delegated design authority |
| AMB-3 | Bypass a stale sealed checkpoint after committing, or restore the correct commit-before-pre-verification order. | The candidate commit changed HEAD after the sealed checkpoint; packet validation rejected the drift and verification state forbids checkpoint rewriting. | material; Codex | AC-7, AC-8, SC-L1 | Commit final evidence bytes while implementing, then seal and verify the final HEAD. | resolved by fail-closed continuity evidence under delegated design authority |

## Confirmation record

- 2026-08-14: user approved autonomous implementation through release and local installation without intermediate confirmation.
- 2026-08-14: revision 2 applies the evidence-backed AMB-1 refinement within that delegated authority; no user-owned outcome or external scope changed.
- 2026-08-14: revision 3 applies the AMB-2 exact-byte portability repair within the same authority; no product outcome or delivery scope changed.
- 2026-08-14: revision 4 applies the AMB-3 verification-order repair within the same authority; no product outcome or delivery scope changed.
