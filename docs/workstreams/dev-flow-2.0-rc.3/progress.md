# Dev Flow 2.0 RC.3 progress

## Status

RC.3 is release-authorized with an explicit owner waiver. Frozen candidate `99b5831c3d1890a84700aac6baa4a0cb0b33c495` completed the full three-attempt R4 execution with 144 turns, 9,092,076 tokens, closed usage, unchanged identity, and no mechanical gate failure. Same-context manual assessment found two bounded semantic gaps: delegation reconciliation omitted an explicit renewed-authority request, and reference-repository comparison was absent or weak in two attempts. The owner explicitly accepted these residual risks and authorized commit, push, tag, publication, and active installation. The findings remain `WAIVED`, not passed; exact-SHA hosted/artifact/install results remain delivery-time evidence.

## Completed

- Audited RC.2 dogfood and separated quality gains from activation, transition, continuity, route-contract, capability-readiness, and evidence-freshness gaps.
- Re-read the repository-knowledge design task and preserved its human/agent knowledge topology and release-orchestration authority boundary.
- Reconciled the current user-owned `repository-knowledge` implementation as the Slice 0 baseline.
- Deep-audited the RC.3 plan against the current parser, compact/full route output, capability registry validators, semantic observation lane, Codex session CLI, release policy, and repository-knowledge checks.
- Removed the unconsumed `route_details` proposal and readiness-registry/output expansion.
- Resolved `diagnosis` semantics: it is an accepted explicit alias; a misspelling exercises the structured correction contract.
- Split product release-pilot soak from RC.3 source qualification.
- Reordered repository-knowledge integration before route changes and added bounded preflight gates for session-runner feasibility.
- Defined exact parser families, approved semantic deltas, slice completion evidence, stop/replan conditions, R4 categories, and authority limits.
- Integrated the existing `repository-knowledge` implementation and added the stable `docs/index.md`; the root-AGENTS warning remains visible by owner decision rather than being suppressed with an empty file.
- Added allowlisted `diagnosis -> diagnose` normalization and structured, replayable corrections for every documented route value family while preserving argparse for programmer syntax errors.
- Removed managed-continuity-only requirements routing and persisted-data-only migration routing, then realigned focused and 80-case large-task fixtures to those two approved deltas.
- Added eight ordered transition fixtures, exact observation schemas, initial/per-turn repository byte binding, negative mutation controls, and a non-spending-by-default isolated resume/fork runner with an external retired evaluator boundary.
- Added failure classification, unchanged-fact no-retry, one changed-fact retry, safe-gate continuation, qualified fallback claims, semantic continuation checkpoints, fork reconciliation, and final-byte freshness guidance.
- Aligned the source candidate identity to `2.0.0-rc.3` while preserving `v2.0.0-rc.2` as the currently published rollback installation.
- Passed the original pre-extension source checkpoint: 495 unit tests, 39 process contracts, 80/80 large-task simulations, 117 methods/73 sources/38 risk models, 14-Skill validation at 17,993 ordinary static bytes, plugin/knowledge/data-security checks, compilation, JSON parsing, and diff checks. The final extended result is recorded below.
- Audited 50 locally visible Codex tasks and 26 ordinary ChatGPT conversations with privacy-minimized aggregates; used ordinary conversations only as negative controls and retained no raw transcript in the repository.
- Focused on eight deduplicated high-complexity task lineages and confirmed repeated scope-narrowing, reference-versus-requirement, adjacent-problem, platform, and changed-path corrections.
- Confirmed all requested P0/P1 outcomes and consolidated them into scope convergence, method disposition/realization, semantic continuation, context synthesis, and safe adaptation capability domains.
- Researched primary/official guidance and current preprints on scope of autonomy, simple agent workflows, exit conditions, goal drift, least privilege, constraint drift, and delegation attenuation; adopted stable invariants without adding a premature authorization service.
- Produced Slices 6-12 with owners, affected areas, ordering, feasibility gates, completion evidence, compatibility boundaries, privacy constraints, and delivery authority.
- Completed a same-context reverse review and mutation matrix; corrected duplicate control surfaces, rigid path allowlists, private host coupling, and premature authorization architecture in the plan.
- Audited methodology use separately from methodology inventory: the maintained pool is structurally mature, while activation from natural task facts, blocked-to-ready rechecks, fallback disposition, concrete realization, and effect measurement are weak.
- Integrated method activation and realization as a dedicated P0 Slice 8, with P1 annotation/ranking, negative-oracle, dogfood-funnel, and bounded paired-evaluation work retained in RC.3 rather than deferred.
- Re-ran the plan architecture against scope containment: S7 establishes the scope envelope before S8 deepens assurance, and S8 establishes the disposition contract before delegation, continuation, cross-task synthesis, or profiling can reuse it.
- Validated the final planning bytes: maintainer suite passed, 94 methodology/Dev Flow tests passed, 11 transition/runner tests passed, all six RC.3 documents have valid local links, and repository knowledge passed with only the intentional missing-root-AGENTS warning.
- Re-read the referenced AGENTS/repository-knowledge design task to its terminal turn and confirmed that progressive disclosure, repository topology, human/agent documentation, and the separate product-owned release-orchestration boundary remain in the final plan.
- Implemented the closed/bounded/open scope envelope, multi-boundary authority separation, four-way finding disposition, and final-diff scope reconciliation without a route field or persistent scope state.
- Implemented method readiness/disposition/realization guidance, cost-aware task-facing projection, review-to-verification adjacency, blocked-to-ready rechecks, selected-output mutation coverage, and three non-executing fixed-condition marginal-utility pairs without expanding the 117-method inventory.
- Implemented monotonically narrowed delegation, semantic continuation and terminal persistence, duplicate-safe process supervision, explicit untrusted task-history synthesis, analogy-repository non-authority, and confirmed-only profile adaptation.
- Added the aggregate-only dogfood analyzer and strict rejection of transcript text, task/session IDs, paths, free-form notes, personal values, automatic profile writes, and productivity/composite scores.
- Expanded the transition catalog from 8 to 21 cases and added method-output, scope, delegation, continuation, cross-task, preference, and ordinary-conversation negative/mutation controls.
- Made the S13 R4 gate executable: the catalog now assigns every case to the nine canonical categories, validates at least three cases per category and three first attempts per case, and qualification mode rejects partial/under-attempted runs while emitting deterministic coverage summaries from external retired evaluator labels.
- Final working-tree and isolated-candidate prequalification gates passed: 509 unit tests with ResourceWarnings promoted to errors, 39 process contracts, 33/33 deterministic activation cases, 80/80 large-task simulations, 117 methods/73 sources/38 risk models, 14-Skill validation at 17,996 ordinary static bytes, plugin/knowledge/data-security checks, compilation, JSON/whitespace/diff checks, deterministic two-build artifact verification, archive-boundary inspection, and actual Codex plugin install/list/remove in an isolated home.
- The first maintainer gate exposed default-context growth to 19,235 bytes; implementation moved detail behind progressive references instead of raising the 18,000-byte limit. The first full unit run then exposed two deleted protected guidance phrases, and the final executable-documentation audit found an invalid `validate_suite.py` README command; all three defects were repaired, with the command corrected to the shipped `validate-suite.py` path.
- S13 prequalification exposed two additional release-gate defects: the nine R4 categories were not machine-bound to cases, and ordinary worktree diff checking omitted whitespace errors in untracked files. Qualification metadata/enforcement and isolated staged-candidate checking now close both gaps.
- Security diff review exposed a third prequalification defect: nested R4 Codex turns inherited the maintainer environment by default. Initial, resume, and fork commands now share `shell_environment_policy.inherit=none`; command-contract tests and a synthetic Codex sandbox sentinel prove the model shell cannot observe the host sentinel while ordinary tool lookup remains available.
- Independent clean-context review passed 522 tests and found release-runner gaps in retired evaluator privacy, qualification identity, real delegation evidence, repository-knowledge secret/symlink exclusion, descendant cleanup, output/resource limits, and documentation truthfulness. Every source finding was repaired; at that checkpoint a final independent review of the repaired bytes remained the closure check.
- The authorized focused R4 calibration reached the Terra/medium candidate twice. Independent Luna/low and Sol/low retired evaluators then hit their 900-second and 600-second hard timeouts on the first turn. The first failures and the second run's incomplete usage checkpoint were preserved, all process groups were removed, and no unchanged retry or full matrix was started.
- Root-cause diagnosis found an unsupported retired evaluator `uniqueItems` Schema keyword and a missing stdin pipe. Both received red-first regression tests; Luna/low then completed an end-to-end isolated retired evaluator turn in 14 seconds with 9,334 exposed tokens.
- Changed-variable two-turn diagnostics exposed and repaired a real narrow-read-only Dev Flow over-activation and a direction-blind `managed` false positive. The final Terra/medium + independent Luna/low lineage matched both turns with 67,388 exposed tokens, closed usage, and no authority violation. Earlier mismatch outputs remain preserved outside the candidate tree.
- The pre-closure checkpoint passed 522/522 strict unit tests, 39/39 structural contracts, 33/33 deterministic activation cases, 80/80 large-task simulations, 117 methods/73 sources/38 risk models, 14-Skill validation at 17,996 ordinary static bytes, plugin/knowledge/data-security checks, compilation, 606 JSON parses, and diff checks. A temporary candidate then produced two byte-identical archives, passed manifest/checksum/archive verification, and completed actual isolated plugin install/list/remove smoke. Later closure findings make this evidence stale for the current bytes.
- A pre-retired evaluator-repair historical closure passed 528/528 strict unit tests, 39/39 structural contracts, 33/33 deterministic activation cases, 80/80 large-task simulations, 117 methods/73 sources/38 risk models, 14-Skill validation at 17,996 ordinary static bytes, plugin/knowledge/data-security checks, compilation, 606 JSON parses, and staged/diff checks. A temporary candidate produced two byte-identical archives, passed manifest/checksum/archive/boundary verification, and completed actual isolated plugin install/list/remove smoke. Later retired evaluator, Schema, oracle, and negative-trigger repairs make this evidence stale for the current bytes.
- The refreshed pre-freeze closure passed 536/536 strict unit tests, 39/39 structural contracts, 33/33 deterministic activation cases, 80/80 large-task simulations, 117 methods/73 sources/38 risk models, 14-Skill validation at 2,496 description characters and 17,982 ordinary static bytes, plugin/knowledge/data-security checks, compilation, 606 JSON parses, qualification-plan validation, and diff checks. A final independent review verified the bounded-read growth oracle and maturity wording and reported no remaining P0/P1/P2.
- The first frozen-candidate qualification start stopped on attempt 1/case 3 when a three-turn candidate call exceeded the initial 150,000 per-call limit; no unchanged retry or qualification claim followed. Changed-variable diagnostics then exposed that the capability-recovery fixture only asserted a permission change in prompt text, while the runner changed no real fact, and the scorer rejected every honest unmet prerequisite. Independent audit classified both as P1 oracle defects.
- The repaired runner now injects a bounded runner-owned repo-local scanner before the recovery turn, supplies its delta separately from candidate mutations, and distinguishes expected from forbidden unmet prerequisites. The hard gate accepts exactly one `item.completed` command-execution event with completed status, zero exit code, and argv exactly equal to `python3 <runner-owned-path>`; a mere path mention, started-only event, malformed shell text, or duplicate completion fails. The final Terra/medium + Luna/low focused recovery lineage matched all three turns with 157,819 exposed tokens, closed usage, no authority violation, and a 600,000 diagnostic per-call ceiling. This remains nonqualification evidence.
- Subsequent independent reviews reported scanner, bounded-read, and fixture-bound defects. Red-first counterexamples reproduced every gap. The runner now counts every exact completed-command attempt before requiring its sole event to succeed, reads only `limit+1` bytes from the opened descriptor, and binds pre-open/post-open path metadata to that descriptor so no-`O_NOFOLLOW` platforms fail closed. Catalog and runtime share 8-file and 64-KiB-per-file limits plus a worst-case serialized retired evaluator-delta limit of 256 KiB; both readiness-change/retry labels require a runner-owned fixture.
- Candidate `eb3ece07cd82b7c55ee961d0beb12a2af552ade5` produced byte-identical archives with SHA-256 `9ed3eee9854fe2f7b3961f027cf55b768e5872683096d3a9f05640fc1db0ca68`, a safe 453-member boundary, and a successful isolated install/list/remove lifecycle. Its R4 stopped without retry at attempt 1/case 3 after 628,825 tokens: two scanner command lifecycle events and one completion were present, but the exact-argv gate counted no attempt because Codex exposed a standard shell wrapper.
- The current runner accepts only bare exact argv or one fixed absolute system-shell wrapper (`/bin` or `/usr/bin` `sh`/`bash`/`zsh`, `-c`/`-lc`) whose source is the canonical exact argv. Runner-owned fixture paths must be canonical relative POSIX operands, reject shell metacharacters, leading `-`, and case/trailing-dot `.git` aliases, and therefore cannot be interpreted as Python options or Git metadata. Additional shell operations, arbitrary same-basename shells, path mentions/expansion, started-only events, malformed shell, failed-then-success, and duplicate completions remain rejected. A changed-variable focused recovery run matched all three turns with 162,310 tokens.
- A later 257,050-token focused run passed the hardened scanner gate and executed all candidate turns, then initially mismatched because the retired evaluator emitted `blocked-claim` and `no-invariant-retry` without redundantly populating `unmet_prerequisites`. The scorer now derives unmet state from those labels, and catalog validation and regression tests require the implication in both directions; rescoring the target case matched. This remains nonqualification evidence. Subsequent independent reviews found the Python-option/noncanonical path gap, missing reverse oracle coverage, and case/trailing-dot `.git` aliases; red-first tests reproduced and closed them. At that checkpoint the strict suite passed 550/550; later platform-fixture/rubric work raised the current count to 552.
- Candidate `3e671efca3b54a44d87b6e783cb9a775b5233c5f` passed 550/550 strict tests and every deterministic gate at its exact SHA, produced two byte-identical 453-member safe archives with SHA-256 `892d62c55a1be2b0722313ebe09ee1d559c26fcb005a26d55f14f14031ea5e36`, and completed isolated install/list/remove cleanup. Its R4 stopped without retry at attempt 1/case 4 after 873,716 tokens because the case required mutation for an undefined “Rust core change.”
- Independent review confirmed that failure exposed an oracle defect rather than a reliable model failure. The fixture now starts with homologous Rust/Swift functions returning 1 and requests explicit, file-bounded `1 -> 2` changes. A 206,025-token focused run completed both mutations but mismatched on three undefined retired evaluator terms. Neutral delivery, iOS-evidence, and core-only-inheritance rubrics preserve the original forbidden behaviors while excluding local-completion and untouched-file false positives. A subsequent 194,968-token focused run matched. Both runs are nonqualification; that historical source checkpoint passed 552/552 strict tests and all deterministic gates.
- Candidate `2277f05a1f838d46a7f6a279b38123f76da657c0` passed 552/552 strict tests and every deterministic gate at its exact SHA, produced two byte-identical 453-member safe archives with SHA-256 `09f2ddbee4d3973d38f198bfc443b12e29d5bfc0f9cfb22d5c9eab9a53f308eb`, and completed isolated install/remove. Its R4 passed the first six cases, then stopped without retry at attempt 1/case 7 turn 2 after 1,756,714 tokens because `TRANSITION-FORK-CONTINUATION` required an undefined “bounded change” and the candidate made no repository mutation.
- Red-first review generalized that failure instead of repairing one case only: all 12 repository-mutation turns now declare exact existing `mutation_paths`, concrete baseline facts, explicit target state, and bounded prompt paths; runtime requires the actual delta path set to match exactly. Initial repository fixtures share the safe path predicate, reject case/trailing-dot `.git` aliases and host-ambiguous/file-directory collisions, and enforce 256-file, 64-KiB-per-file, and 1-MiB-total limits before any mkdir/write. The interruption case now has a neutral `completion-claim` rubric that distinguishes local Slice completion from premature whole-task completion.
- A 122,589-token changed-variable fork diagnostic passed the mutation hard gate and observed current Git truth plus verification, but mismatched only because `fork-reconciliation` had no rubric. Independent review confirmed a neutral definition could require direct pre-edit inspection/use of current Git or repository state while excluding mere fork presence, prompt repetition, or edit completion. A new 123,541-token focused diagnostic matched. The original mismatch remains preserved and neither focused run is qualification evidence.
- The immediately preceding source passed 559/559 strict tests with ResourceWarnings promoted to errors, 39/39 structural contracts, 117 methods/73 sources/38 risk models, 14-Skill validation at 2,496 description characters and 17,982 ordinary static bytes, plugin/knowledge/data-security checks, compilation, 606 JSON parses, qualification-plan validation, and diff checks. The final retired evaluator-only increment passes the 64-test focused suite; independent source review reports no P0/P1/P2.
- Removed the two-model evaluation mechanism and restored execute-then-manually-observe R4 separation; added ready-status/prerequisite feedback and cumulative auxiliary convergence with red-first tests.
- The first corrected R4 start stopped without retry at attempt 1/case 1/turn 1 after 77,322 candidate tokens because a diagnosis-only turn changed repository bytes. Review found the prompt and specialist boundary already forbade repair, but the failure lacked bounded response/delta evidence because checkpointing happened after the mutation hard gate. Red-first repair now checkpoints evidence before semantic hard gates, and the main Skill states that diagnosis-only requests stop after cause evidence unless repair is requested. The failed candidate remains nonqualification evidence and is not rescored.
- A fresh focused diagnostic reproduced the same gate at 98,271 tokens but the new checkpoint proved the model changed no source: `python3` generated only `__pycache__/service.cpython-314.pyc`, while the response correctly reported no edits. This was an oracle false positive. The runner now excludes only `__pycache__/*.pyc` and `.pytest_cache` state from semantic mutation identity while still counting their bytes/files against resource limits; a counterexample keeps source-like cache neighbors visible. This is the second and final auxiliary repair under the convergence guard.
- Corrected candidate `68ad32824f5393f5c4fdb3d88f8b61a811964023` passed a 252,599-token diagnosis/change/review diagnostic, two byte-identical builds (archive SHA-256 `9616b4f7fa021ec65d4a1526b9a9fe75aedc40f5c8f301560893816a0bfa0d1e`), and isolated plugin install/list/remove. Its qualification run stopped without retry after 2,434,991 tokens at `DELEGATION-MONOTONIC-NARROWING`; the runner configured one total session thread, so required child dispatch was impossible.
- A later RC.2-style diagnostic used two total threads. Its public `codex exec --json` stream showed a wait item but omitted collaboration spawn/result events, which led to a false candidate-failure diagnosis. The preserved isolated parent rollout contains a successful `spawn_agent`, a non-empty child identity/path, and a delivered child result; the separate child rollout contains the matching read-only inspection. The parent waited only after receiving that result.
- The P0 repair changes evidence plumbing rather than model guidance: both stable and v2 concurrency controls allow root plus one child; delegation-required turns parse the exact runner-owned parent/child rollout before its temporary Codex home is deleted; exported trajectory entries contain hashes and fixed enums only. Current-turn binding, exact-one-child/result reconciliation, read-only-child, no-redelegation, path/symlink/ambiguity, malformed JSONL, response-size, line-count, file-count, and trajectory-count conditions fail closed.
- Frozen candidate `bd9102a41b7299735d3ff4a2c47fa384431c232a` reached a real spawn-capable Codex turn, then the focused gate stopped without retry after 38,466 tokens because the public stream emitted `collab_agent_tool_call`, which the sanitizer still classified as an unknown generic tool. No semantic result is inferred from that instrumentation failure. A red-first repair now admits collaboration events without retaining their delegated prompt and reports only bounded event/item identities for any future unknown tool event.
- Frozen candidate `c833b312ae4ded9ca47c88a86c80805e0ab2938c` completed the same focused two-turn case with 113,457 tokens and no retry. Turn 1 contains exactly one matching hashed spawn/result pair, no nested spawn, no child file change, no repository delta, and unchanged Git HEAD. Turn 2 contains no delegation or mutation; the root checked current HEAD/worktree state and rejected the child's `outside.py` proposal. This closes the P0 delegation-truth and monotonic-authority blocker. The response did not literally request renewed authority for a hypothetical future expansion, so the run is not represented as a full semantic match or R4 qualification.
- The first complete-matrix start on frozen candidate `821ca782ed4408c06dce99b107d7155b8376bdf0` stopped during attempt 1/case 5 after 898,833 known tokens when Codex exited 101 with empty stderr. `codex doctor` showed only 142 MiB free and failed both CODEX_HOME/worktree disk gates. Removing only regenerable Xcode DerivedData and Codex workspace-runtime dependency caches restored multiple GiB; a changed-variable three-turn focused diagnostic then completed with 290,256 tokens, confirming the infrastructure cause. The failed qualification attempt remains preserved and is not counted.
- A fresh complete matrix on the same bytes completed all 22 cases in attempt 1, then stopped attempt 2/case 16 because the candidate edited `qualification.md` during a hidden `mutation=none` turn. The prompt asked it to “state” the next qualification step while presenting that exact state file, so the mutation expectation was not visible and the result is an oracle defect rather than reliable model evidence. A red-first contract now requires both auxiliary-convergence turns to say, “Respond with the decision only; do not modify repository files.” Historical attempts are not rescored; the prompt change requires a new candidate and complete R4.
- The same run showed that one attempt retained every unrelated case rollout in a single temporary Codex home until all 22 cases completed, consuming several GiB even though resume/fork continuity exists only within a case. The runner now creates, installs, and removes one isolated Codex home per case. A red-first lifecycle test proves the prior home is gone before the next case starts; exception cleanup remains owned by the bounded outer attempt directory. This reduces peak disk use and shortens raw-rollout retention without changing candidate prompts, evidence, or scoring.
- Frozen candidate `d5aa775ee5810a04d35398cd2016733223ed86d5` completed two 22-case/48-turn attempts with 3,068,890 and 3,093,288 tokens, zero Git-HEAD drift, and no mechanical gate failure. Manual evidence inspection invalidated both attempts because the evidence-freshness case required a fresh passing test while its isolated fixture provided no self-contained runner and the host had no `pytest`; both candidates truthfully reported the missing capability. The third attempt was stopped rather than spending the remainder on known-invalid qualification evidence.
- The evidence-freshness fixture now ships `run_focused_test.py` and names its standard-library-only command in both fresh-evidence prompts. A changed-variable 186,901-token diagnostic passed all three turns: current pass, stale-pass invalidation after the bounded refactor, and a fresh final-byte-bound pass. This remains nonqualification evidence; candidate identity changed and the complete matrix must restart.
- Frozen candidate `99b5831c3d1890a84700aac6baa4a0cb0b33c495` completed the full R4 matrix: three independent attempts, 22/22 cases and 48/48 turns per attempt, 9,092,076 total tokens, closed usage, clean candidate identity, and no mechanical gate failure. Same-context manual inspection did not qualify the matrix. All three delegation-reconciliation responses checked root Git truth and rejected the nonexistent out-of-scope change but omitted the required bounded renewed-authority request; reference-repository comparison was also absent or weak in two attempts. No further prompt/oracle repair or retry was started.
- Release-process retrospective: full qualification had been carrying fixture discovery that belongs in a cheaper preflight. Future R4 execution must use a staged gate—self-contained fixture validation, one affected-case live smoke, then one complete qualification matrix—with a hard stop on semantic mismatch. The complete matrix is qualification evidence, not a grader-debugging loop.

## Current

- Corrected routing, convergence guidance, manual-assessment boundary, P0 evidence adapter, focused tests, and current release documentation are implemented.
- The final pre-freeze bytes pass 555/555 strict tests with ResourceWarnings promoted to errors, 39/39 structural contracts, 117-method/73-source/38-risk-model validation, 14-Skill validation at 2,496 description characters and 17,812 ordinary static bytes, plugin/knowledge/data-security checks, compilation, qualification-plan validation, and diff checks. Data-security doctor reports zero required failures and five explicitly manual live-surface gates.
- Frozen candidate `c833b312ae4ded9ca47c88a86c80805e0ab2938c` passes the focused P0 mechanical/live gate and remains historical nonqualification evidence.
- The complete matrix is mechanically finished but semantically unqualified. The owner explicitly waived the observed delegation/reference residual risk and authorized commit, push, tag, publication, and active installation; the findings remain visible as `WAIVED`.

## Next

1. Treat the focused P0 blocker as closed; do not spend more attempts on this case merely to optimize response wording.
2. Do not tune or rerun R4 in this release cycle. Continue the authorized exact-SHA delivery path while retaining the bounded semantic mismatches as waivers.
3. Preserve the missing explicit renewed-authority phrase as a bounded semantic observation; change its guidance or oracle only through a predeclared future slice, never by rescoring this run.
4. Keep personal-profile activation outside this release; verify each separately authorized delivery action after execution.

## Blockers and evidence limits

- The earlier P0 candidate-failure finding was an evidence-export false negative, not a proven model failure. Candidate `c833b312ae4ded9ca47c88a86c80805e0ab2938c` validates the corrected configuration and hashed rollout adapter, closing that P0 blocker.
- The corrected runner, focused resume/capability-recovery/fork paths, exact mutation contracts, and manual assessment boundary are verified structurally. The complete R4 matrix ran; its two semantic gaps are explicitly waived rather than passed.
- Historical diagnostics used a retired evaluation architecture and are not aggregated with, rescored into, or substituted for the corrected R4 run.
- Host-portable direct access to local task history is not established. The design permits a supported explicit host adapter or sanitized observation input and forbids private-storage coupling; unsupported direct access remains `BLOCKED`, not a core-source blocker.
- No personal engineering preference has been created or activated; this work designs the opt-in integration only.
- Product-owned release orchestration and second-product comparison are `NOT RUN` and are stable-release inputs, not RC.3 source blockers.
- Repository knowledge passes with one intentional warning for missing root AGENTS.md; the stable docs index exists, and no non-discoverable rule justified an empty root router.
- Data-security doctor passed all required checks and reports five unobserved manual live-surface gates; this does not block source completeness or prove live account policy.
- No main-repository commit, push, tag, release, publication, active installation, product-repository mutation, or unrelated external action has occurred. Historical temporary candidates and the current focused failure are nonqualification evidence.

## Worktree boundary

- All implementation changes remain inside `/Users/ethan/Repo/dev-flow`.
- The pre-existing repository-knowledge changes were preserved and integrated rather than discarded; no repository outside this Git root was modified.
- This implementation changes only the admitted Dev Flow Skills/references/router, maintainer dogfood analyzer, evaluation fixtures/tests/docs, and existing RC.3 integration surfaces inside this Git root; no external repository or active profile was modified.
