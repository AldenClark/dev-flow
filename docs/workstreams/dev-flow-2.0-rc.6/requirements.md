# Dev Flow 2.0 RC.6 requirements

## Outcome

Publish the current benchmark-separation and release-truth repairs as `2.0.0-rc.6`, without representing candidate bytes as already published.

## Confirmed context

- The version owner authorized RC.6 commit, push, exact-SHA CI, artifact evidence, tag, public prerelease, and isolated-profile installation on 2026-09-01.
- `v2.0.0-rc.5` is the current public immutable RC and the RC.6 rollback target.
- Live model execution remains outside this delivery authority and is not needed because the candidate does not alter a directly affected model-facing semantic case.
- An independent clean-context reviewer was not separately authorized; the release retains a same-context static review and its common-mode limitation.

## Required behavior

1. The source candidate, latest published RC, workspace base, manifest, maintained release surfaces, and delivery state remain aligned and machine-validated.
2. Doctor reports cache, CLI registration, loaded identity, and Hook packaging/activation as separate bounded observations.
3. Repeated/comparative model research remains in Dev Flow Bench and is not a release tier or release verdict.
4. RC.6 uses the R2 evidence path: final local semantic suite, applicable hosted compatibility, isolated fresh install/uninstall, exact-SHA artifact evidence, immutable tag, and public prerelease verification.

## Limits

- Local checks do not prove hosted CI, public release, attestation, installation, or marketplace activation.
- RC.5 evidence is historical and cannot qualify RC.6 bytes.
- Same-context review is not independent review.
