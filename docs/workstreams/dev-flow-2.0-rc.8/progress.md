# Dev Flow 2.0 RC.8 progress

## Current truth

`v2.0.0-rc.8` is released from exact candidate `503f5e487580afed9fb905130c4206e1e018ed1d`. The annotated tag, public prerelease, exact-SHA hosted qualification, verified and attested artifacts, and isolated public-tag installation are complete. The maintainer's primary Codex plugin separately reports enabled version `2.0.0-rc.8` from the pinned Git marketplace; its manifest and data-security Hook bytes match the tag. This is installed-file evidence, not a claim that every running Codex session has reloaded every RC.8 component. The stable 2.0 release and production effect remain separate.

- Release source: candidate `503f5e487580afed9fb905130c4206e1e018ed1d` is the immutable `v2.0.0-rc.8` tag; the post-publication current-truth commit is separate and does not change tag bytes.
- Release tier: R2, because model-facing guidance and public CLI/runtime behavior changed. Exact-SHA semantic CI, the focused platform matrix, artifact integrity, publication, and isolated install passed. The two incomplete affected model-semantic cases are explicit RC prerelease waivers, not passes.

Delivery state: commit=passed; hosted_ci=passed; cross_platform=passed; independent_review=passed; tag=passed; artifact=passed; publication=passed; isolated_install=passed.

The plan incorporates all five previously agreed RC.8 directions: target continuity, project-native feedback, real-environment delivery, scope-aware multi-agent convergence, and structural/packet-era cleanup. GPT-6 instruction debt and model routing are explicit enabling slices. The user observation about more focused but less spontaneous exploration is a hypothesis; independent benchmark results are mixed, especially for Luna, and local Luna evidence is absent.

## Plan-audit disposition

These are three local self-audit passes of the proposal, not independent model trials or a code review of an RC.8 implementation.

1. **Scope/evidence audit:** confirmed the five prior directions, technology-neutral owner boundaries, no Bench expansion or third-party Skill recommendation, and a clear split between official guidance, third-party results, user impression and local task anecdotes. Disposition: accepted for planning; behavioral efficacy `NOT RUN`.
2. **Adversarial routing audit:** tested the proposed decision table against closed high-risk work, open semantics, uncertain diagnosis, ordinary review, critical acceptance and model unavailability. Corrected the initial temptation to route every `security`/`ffi` or high-risk acceptance label to Astra; consequence now raises evidence depth while compound unresolved reasoning raises the model tier. Disposition: policy coherent; deterministic route implementation/tests `NOT RUN`.
3. **Implementation-readiness audit:** mapped every slice to existing owners, order, negative controls, authority boundaries and candidate gates. Kept `implementation.md` as stable plan and this file as mutable evidence; did not change product-state. `check-workstream` returned `not-applicable` for this unmarked workstream (not a PASS); the repository-knowledge check passed with zero errors/warnings, product-state validation remained valid at RC.7, and the maintainer suite validator passed with 15 Skills and 14 routing cases. Disposition: ready to implement in bounded slices, not ready to claim RC.8 quality or release.

## Source implementation and review

- **RC8-1:** Main lifecycle and requirements owners now make U1 a detailed-understanding boundary, pausing only for an unresolved material user choice or requested review. `route-task --user-choice-open` represents that condition; a clear U1 can continue. Focused exploration and completion are conditional rather than mandatory ceremony.
- **RC8-2:** Child route P0–P3 = GPT-6 Luna, P4–P5 = GPT-6 Sol, P6/PX = GPT-6 Astra. Risk remains an evidence/review input rather than an automatic Astra trigger. Compound P6 rules, exact profile vectors, availability reporting, and no-dispatch on unchecked/unavailable host capability have deterministic positive/negative cases. The root Sol-medium default is guidance, not a runtime switch. A caller-supplied host inventory must reflect the real dispatch host; model spend is not authorized by routing. With explicit user approval, the development paired-evaluator identity now uses GPT-6 Sol for inventory/assembler/grader at the prior medium/high/medium efforts and binds observed local `codex-cli 0.154.0` darwin-arm64 bytes; no pilot was executed.
- **RC8-3/6:** Continuity output identifies terminal reconciliation and scope-change invalidations, including stale descendant results. A changed user choice, risk, repository fact, method, review need, or caller-bound objective/scope epoch now yields `reject-until-rebased`; unchanged routes without an objective/scope epoch require reconciliation before child-result reuse. New method/review obligations also invalidate old terminal evidence. Negative controls failed before repair and passed after. Skills specify objective/slice/path/oracle/resource ownership and late-child rejection. An actual steered multi-agent run remains `NOT RUN`.
- **RC8-4/5:** Native-feedback and delivery owners now require actor-bound user-outcome oracles and applicable final-bytes → artifact → target → effective-state → effect → observation/recovery links. Synthetic `unittest` negative controls demonstrate that background-only success, wrong installed bytes, and stale configuration fail a strengthened oracle. They do not prove the sampled real app/device or any deployment was repaired.
- **RC8-7:** Both direct and supported parsers now construct exactly the 17 public commands; the supported entrypoint retains structured `unsupported` responses for packet command names. Direct parser/help negative controls failed before the retirement and pass afterward. The active lower-level `select-methods` study moved to a dedicated maintainer script: normal, alias, invalid, and non-default-root calls were compared against the previous handler, and focused maintainer tests now exercise the new path. The separate cleanup removed the packet command handlers, 89 AST-unreachable packet helper functions, 50 now-unused packet constants and obsolete packet-only tests; `dev_flow.py` fell from roughly 8,000 to 3,030 lines. A test inventory distinguished 92 packet-dependent, two mixed, and one 2.0-only method; useful mixed assertions moved to active route tests, and a linked-worktree assertion now tests Git local-exclude behavior directly. A second caller inventory found `dependency_contracts.py` entirely unimported and the old `engineering_context.assess_context` graph unreachable from supported commands. Those internals, the packet-only readiness tests, and the misleading `profile-tool.py suppress` writer and guidance were retired together; active profile resolution, risk vocabulary, `waive`, and route tests remain. A negative test first failed on the still-exposed `suppress` command and passes after removal. Two orphan readiness schemas were removed; the capability-admission schema was restored when a required historical migration contract exposed its continuing traceability role. `validate-flow-packet.py` was retired because its command target no longer exists. Top-level templates remain: `init-workstream` consumes workstream templates, `run_contract_checks.py` reads `templates/context.md`, and other fixtures still participate in maintained static contracts; blanket deletion would be unsafe. Repository searches and deterministic tests cannot prove absence of external direct callers, so the public-entrypoint boundary remains explicit.

Independent read-only audits found route-contract gaps: unchecked host inventory still emitted `delegate: true`; a swapped P0/P6 vector passed registry validation; an unchanged flag digest could falsely retain a child result after an objective/scope correction; and new method/review obligations did not invalidate terminal evidence. Each has a negative control and focused repair. The first full contract run also caught a missing `user-interaction.md` reference in the compact main Skill; it was restored and the 39 contracts passed. An audit is not live model or target-environment proof.

`evals/paired-evaluations.json` is now a GPT-6 Sol development evaluator identity, separately versioned by its changed config bytes; the prior GPT-5.6 development results retain their historical config identity. `paired-evaluations-acceptance.json` remains the frozen RC.7 acceptance identity on GPT-5.6. Changing that frozen config in dirty RC.8 development would not provide release qualification. GPT-6 attested pilot and RC.8 acceptance qualification are `NOT RUN`; config migration does not prove evaluator quality or model behavior.

## Release closure and remaining limits

- Exact candidate `503f5e487580afed9fb905130c4206e1e018ed1d` passed [hosted semantic CI and the focused Ubuntu/macOS/Windows matrix](https://github.com/AldenClark/dev-flow/actions/runs/36098484599). Local candidate checks passed 644 tests with one skip, 39 contracts, required validators, compilation, and diff checks; independent source review passed.
- [Release-evidence run 36098724832](https://github.com/AldenClark/dev-flow/actions/runs/36098724832) built the archive, SPDX 2.3 SBOM, checksums, and manifest from those bytes. Downloaded artifacts verified against version and commit, and provenance/SBOM attestations verified. The public archive SHA-256 is `73466ad0098ae9d091376ae60f9050a248605053b14ea14079dca71b51aff6a4`.
- The annotated `v2.0.0-rc.8` tag resolves to the candidate. The [public prerelease](https://github.com/AldenClark/dev-flow/releases/tag/v2.0.0-rc.8) was observed with four matching assets. A fresh temporary Codex home installed `dev-flow@dev-flow` from that public tag, discovered 15 Skills, and removed it cleanly. The primary Codex marketplace is pinned to RC.8 and the enabled installed plugin reports `2.0.0-rc.8`; its manifest and data-security Hook hashes match tagged source. No Homebrew Codex CLI was installed or used for this verification.
- Bounded GPT-6 Sol `NEGATIVE-ORDINARY-CONVERSATION-QUIET` completed and matched. `SCOPE-CLOSED-DEEP-IMPLEMENTATION` did not finish: turn two exceeded the bounded Bench budget on two attempts. `TRANSITION-UNCHANGED-FOLLOWUP-NEGATIVE` has no complete App-CLI result after interrupted earlier attempts. The latter two are `WAIVED` for this RC prerelease, never `PASSED`. There is no controlled Luna/Astra comparison or stable-release semantic qualification; those remain `NOT RUN`. The source-only negative controls and host tests do not establish production effect or a fully reloaded existing Codex session.
- The primary-profile upgrade briefly exposed a stale RC.7 Hook path when plugin removal deleted old cache during an active session. The user manually reinstalled RC.8; subsequent App-bundled Codex commands and this task's tools operated normally. Root cause of the earlier macOS Gatekeeper popup was not established, and no bypass or separate Homebrew CLI installation was performed.

### Candidate-gate record (historical)

The following paragraphs record the pre-publication source checkpoint, not the current release state.

Product-state validation is valid for the RC.8 source candidate with RC.7 still published. Knowledge, plugin, data-security required checks, methodology, RC.4/RC.5 static checks, 39 process contracts, focused route tests, Python compilation, and `git diff --check` passed after the cross-module cleanup. The first contract run failed because the historical capability-admission migration target was missing; restoring that schema made all 39 pass. Full `unittest` discovery on the candidate worktree passed 644 tests with one skip; the lower count than earlier RCs is due to retired packet/readiness-only cases, not skipped failures. The development evaluator backend identity was independently checked against the actual local executable without a model call. The maintainer suite validator passed with 15 Skills and 32 agent-dispatch cases, with the ordinary static budget within its 13,500-byte target but above its 12,500-byte stretch target. The data-security doctor has five non-required live/manual surfaces marked `not_observed`. `check-workstream` remains `not-applicable` for this unmarked workstream, not a PASS. Independent source reviews found and verified repairs for target/scope and terminal-evidence invalidations, compared the migrated selector against the previous handler, and found no supported 2.0 caller lost by command retirement. Final release-diff clean-context review caught a stale README P3–PX model mapping, which was corrected and guarded by a registry-derived test; its focused recheck found no further supported-path issue. This is static source evidence, not a live-model or hosted result, and cannot rule out external direct imports of old internals.

The integrated RC.8 behavior/evaluator slice began at local commit `1a834ec`; the reviewed packet cleanup and candidate projections are now locally committed too. The published RC.7 identity is unchanged. Live Luna/Sol/Astra comparison, hosted/platform acceptance, paid evaluator execution, and external delivery stay `NOT RUN`. Candidate source identity is not release readiness; model spend, push, tag, workflow dispatch, publication, and primary installation remain separate actions. Do not infer external consumers are absent from a repository-only search.
