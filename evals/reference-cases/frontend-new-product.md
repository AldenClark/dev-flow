# New frontend product reference case

Fixture assumptions: the repository contains a backend contract and an empty frontend shell but no authoritative design assets. A good first attempt treats the visible work as material, uses co-design, discovers users/tasks/product constraints, recommends a direction, chooses only the fidelity needed to resolve uncertainty, and records UX Ready before production implementation.
