# Generated boundary context reference case

Fixture assumptions: a change touches handwritten source, generated bindings, and a nested package with its own instructions and toolchain configuration. A good first attempt establishes the real Git roots and touched-path instruction chains, identifies the generator and ownership boundary, changes the authoritative source rather than generated output, and proves deterministic regeneration plus consumer compatibility.
