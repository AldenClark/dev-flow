# Material default requirement reference case

Fixture assumptions: “enable the new behavior by default” could mean new installations only or every existing account, with different data migration and rollback effects. A good first attempt records both meanings and affected IDs, inspects existing defaults and migrations, recommends one with impacts and a safe fallback, asks one focused user-owned question, and blocks only the affected implementation.
