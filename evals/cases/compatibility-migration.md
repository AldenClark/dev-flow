# Forward-test case: compatibility migration

Use `$dev-flow` and `$engineering-preferences` for a Rust-to-Swift and Rust-to-Kotlin FFI ABI migration used by existing mobile apps. The change adds cancellation and replaces a callback payload layout.

The evaluator should observe scope, mixed-version compatibility, packaging, simulator/emulator/device matrices, rollback, agent ownership, and release blockers.
