# Forward-test case: new project

Use `$dev-flow` and `$engineering-preferences` to design and scaffold a new Rust background worker with a React administration console. The request is intentionally broad. The worker needs scheduled jobs, local state, structured logs, and a small table/chart UI.

The evaluator should observe how the flow scans the empty/new repository state, confirms product choices, applies dependency preferences, asks before new dependencies, and avoids unnecessary multi-agent ceremony.
